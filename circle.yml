machine:
  environment:
    PYENV_VERSION: 3.6.4
    TOXENV: py36
  override:
    - cd /opt/circleci/.pyenv && git pull
    - for i in ${PYENV_VERSION//:/ } ; do pyenv install -s $i ; done
test:
  override:
    - tox -v
  post:
    - codecov
