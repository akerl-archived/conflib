machine:
  services:
  - docker
  environment:
    PYENV_VERSION: 3.7.0
    TOXENV: py37
dependencies:
  override:
  - /bin/true
test:
  override:
  - docker run -ti -e PYENV_VERSION -e TOXENV -v $(pwd):/opt/build -w /opt/build dock0/python /opt/build/scripts/test.sh
